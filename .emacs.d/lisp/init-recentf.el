;;; init-recentf --- -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:
(recentf-mode 1)
(setq recentf-max-saved-items 1000
      recentf-exclude '("/tmp/" "/ssh:"))


(provide 'init-recentf)
;;; init-recentf.el ends here
